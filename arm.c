#include <stdio.h>
#include <conio.h>
#include <math.h>
void main()
{
  int number,a,result,b;
scanf("%d",&number);
  while(number!=0)
  {
  a=number%10;
  result=0+a*a*a;
  b=number/10;
  a=b;
while(b==0);
}

//checking, the main partresult=a;
        if (result ==number)
      {
  printf("%d", number);
      printf("it is the armstrong number::");
      }
            else //check this also
            printf("%d its not being the armstrong number::",number);

getch();
}
